name: safe_road
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ---------------------------
  # 🔥 Firebase Packages
  # ---------------------------
  firebase_core: ^2.24.2
  firebase_auth: ^4.17.4
  cloud_firestore: ^4.15.8
  firebase_storage: ^11.7.4
  firebase_messaging: ^14.9.2
  firebase_database: ^10.4.0   # ✅ ADDED (Realtime DB)

  # ---------------------------
  # 📍 Mapping & Location
  # ---------------------------
  flutter_map: ^8.1.1
  flutter_map_animations: ^0.9.0
  latlong2: ^0.9.0
  geocoding: ^2.1.0
  geolocator: ^10.1.0
  google_maps_flutter: ^2.12.3

  # ---------------------------
  # 🎥 Media & Camera
  # ---------------------------
  camera: ^0.11.0+1
  video_player: ^2.10.0
  video_thumbnail: ^0.5.6
  just_waveform: ^0.0.7

  # ---------------------------
  # 📡 Network & Utilities
  # ---------------------------
  http: ^1.6.0
  device_info_plus: ^10.0.1
  permission_handler: ^11.3.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.5
  csv: ^5.0.2
  cupertino_icons: ^1.0.8

  # ---------------------------
  # 🔔 Notifications
  # ---------------------------
  flutter_local_notifications: ^17.2.1  # ✅ ADDED
  vibration: ^3.1.4                      # ✅ ADDED

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/ambulance.png
    - assets/images/hospital.png
    - assets/images/police.png
    - assets/images/fire_brigade.png
    - assets/images/public.png
